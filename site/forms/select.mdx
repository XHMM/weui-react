---
name: WeUIFormSelect
menu: Components
---

import { Playground, Props } from 'docz'
import { WeUIForm, WeUIFormSelect } from '../../build'
import "../../build/index.css"

## Props
<Props of={WeUIFormSelect}/>

## Usage
<Playground>
    {
      () => {
        return (
          <WeUIForm>
            <WeUIFormSelect
                      label="time"
                      options={new Array(12).fill(0).map((_, idx) => ({ label: `${idx + 1}`, value: `${idx + 1}` }))}
                      onChange={() => {}}
                      value={3+""}
                      adornment="month(s)"
            />
          </WeUIForm>
        )
      }  
    }
</Playground>
